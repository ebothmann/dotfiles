- clean: ['~']

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [mkdir -p ~/.zgen, Creating zgen directory]
    - [tic xterm-256color-italic.terminfo, Setting up italics support]

- link:
    ~/.zshrc:       zshrc
    ~/.zsh:         zsh/
    ~/.gitconfig:   gitconfig
    ~/.vimrc:       vimrc
    ~/.vim:         vim
    ~/.tmux.conf:   tmux.conf
    ~/.config/tmux: tmux
    ~/.inputrc:     inputrc
    ~/.editrc:      editrc
